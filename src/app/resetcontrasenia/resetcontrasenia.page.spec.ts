import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ResetContraseniaPage } from './resetcontrasenia.page';

describe('ResetContraseniaPage', () => {
  let component: ResetContraseniaPage;
  let fixture: ComponentFixture<ResetContraseniaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ResetContraseniaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
